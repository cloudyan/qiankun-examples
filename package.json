{
  "name": "qiankun-examples",
  "private": true,
  "version": "1.0.0",
  "description": "qiankun 对接案例",
  "scripts": {
    "preinstall": "npx only-allow pnpm",
    "dev:all": "lerna run dev --concurrency=10 --ignore=docs",
    "dev": "lerna run dev --scope=@example/master-umi4 --scope=@example/slave-umi4 --scope=@example/slave-umi3 ",
    "client": "lerna run dev --scope=@example/slave-umi4 --scope=@example/slave-umi3 ",
    "build": "lerna run build --ignore=docs",
    "test": "lerna run test"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "lerna": "^7.3.0",
    "nx": "^16.8.1"
  }
}
